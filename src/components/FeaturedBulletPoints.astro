<div>
    <slot />
</div>

<style>
    div {
        display: flex;
        flex-direction: column;
        gap: var(--space-m);
    }

    @media screen and (min-width: 1000px) {
        div {
            flex-direction: row;
            justify-content: center;
            gap: var(--space-s);
        }
    }
</style>
