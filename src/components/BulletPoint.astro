---
const {icon, text} = Astro.props;
---

<div>
    <i class={icon}></i>
    <p>{text}</p>
</div>

<style>
    div {
        display: flex;
        align-items: center;
        gap: var(--space-s);
        color: var(--black-1);
    }
    i {
        font-size: 1.5625rem;
    }
</style>
